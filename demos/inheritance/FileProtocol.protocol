package demos.inheritance;

typestate FileProtocol {
    Init = {
        void open (): Open
    }

    Open = {
       byte read (): Open,
       void miau (): Open,
       void close (): end
    }
}
